// Font loading configuration for expo-font.
//
// To add custom fonts:
// 1. Drop ClashDisplay.otf into assets/fonts/
// 2. Drop JetBrainsMono.ttf into assets/fonts/
// 3. Uncomment the two lines below
//
// The app uses system fonts as graceful fallback.

export const fontAssets: Record<string, number> = {
  // 'ClashDisplay': require('../../assets/fonts/ClashDisplay.otf'),
  // 'JetBrainsMono': require('../../assets/fonts/JetBrainsMono.ttf'),
};
